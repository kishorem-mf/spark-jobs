
test {
  db {
    h2testdb {
      driver = "org.h2.Driver"
      url = "jdbc:h2:file:./target/h2/test/mark_completed"
      user = "user"
      password = "pass"
      # Connection Pool settings
      poolInitialSize = 10
      poolMaxSize = 20
      connectionTimeoutMillis = 1000
    }
  }
}
